package service;

import model.AuthData;
import model.UserData;

public class UserService {
    public AuthData register(UserData user){
        //Pretend like this actually works
        return new AuthData("ugood", "taho");
    }
}
